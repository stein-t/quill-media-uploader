{
  "name": "quill-media-uploader",
  "version": "1.0.1",
  "description": "Quill module for uploading any media.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib/**/*",
    "src/assets/quill.upload.scss"
  ],
  "author": "Thomas Stein",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/stein-t/quill-media-uploader/issues"
  },
  "homepage": "https://github.com/stein-t/quill-media-uploader#readme",
  "dependencies": {
    "@fortawesome/fontawesome-free": "^5.15.4",
    "quill": "^1.3.7",
    "rxjs": "^7.3.0"
  },
  "devDependencies": {
    "@types/quill": "^1.3.10",
    "tslint": "~6.1.0",
    "typescript": "~4.3.5"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "lint": "tslint -p tsconfig.json",
    "publish-lib": "npm run build && npm publish",
    "publish-beta": "npm run build && npm publish --tag beta",
    "publish-dryrun": "npm run build && npm publish --dry-run"
  }
}
